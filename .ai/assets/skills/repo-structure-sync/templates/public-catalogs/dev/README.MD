# Project Knowledge

This directory contains target-repository governance and project knowledge. `repo-structure-sync` refreshes target-specific navigation after inventorying repository-native evidence.

Use `INDEX.md` for the catalog. Keep reusable AI assets under `.ai/`, durable observations under `assessments/`, cross-workflow work under `backlog/`, and execution records under `workflows/`.
