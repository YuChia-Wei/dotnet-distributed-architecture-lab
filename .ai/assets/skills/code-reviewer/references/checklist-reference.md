# Code Review Checklist Compatibility Entry (.NET)

This path is retained for the `v0.13.x` migration window. It is not a semantic
owner and must not be loaded as a second checklist.

Use the canonical route contract instead:

- `.ai/assets/skills/code-reviewer/references/review-routing.yaml`

That contract selects the file-type standard first and adds stable engineering
rule IDs only for applicable findings. Removal of this compatibility entry is
reviewed at `v0.14.0`.
