{
  "useCase": "Restock",
  "behavior": "Return stock to inventory and publish an inventory integration event when successful.",
  "input": [
    { "name": "productId", "type": "Guid" },
    { "name": "quantity", "type": "int", "constraint": "> 0" }
  ],
  "aggregate": "InventoryItem",
  "aggregateId": "Guid",
  "method": "InventoryItem.Restock",
  "domainEvent": "InventoryControl.Domains.DomainEvents.StockReturned",
  "repository": "IInventoryItemDomainRepository for load plus IInventoryStockOutbox for atomic state/event commit",
  "inputType": "RestockInput",
  "output": "Result<RestockOutput>",
  "domainModelNotes": [
    "InventoryItem is looked up by ProductId in the application layer.",
    "The current application handler name contains a typo but the business behavior is restock/return stock."
  ],
  "domainEvents": [
    "InventoryControl.Domains.DomainEvents.StockReturned",
    "Lab.BoundedContextContracts.Inventory.IntegrationEvents.ProductStockReturnedIntegrationEvent"
  ],
  "constructorPostconditions": [
    "Current stock is increased by the returned quantity.",
    "IInventoryStockOutbox atomically updates stock and inserts ProductStockReturnedIntegrationEvent with a UUID v7 message ID and ProductId N partition key.",
    "The update matches the expected pre-mutation stock and fails closed on a concurrent change."
  ],
  "requirements": ["INV-003", "INV-004", "INV-009", "INT-004"],
  "qualityUplift": "Current aggregate permits non-positive restock values; reconstruction must reject them without side effects."
}
