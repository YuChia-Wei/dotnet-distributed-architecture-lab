{
  "useCase": "IncreaseStock",
  "behavior": "Increase available stock for a product and publish an inventory integration event when successful.",
  "input": [
    { "name": "productId", "type": "Guid" },
    { "name": "quantity", "type": "int", "constraint": "> 0" }
  ],
  "aggregate": "InventoryItem",
  "aggregateId": "Guid",
  "method": "InventoryItem.IncreaseStock",
  "domainEvent": "InventoryControl.Domains.DomainEvents.StockIncreased",
  "repository": "IInventoryItemDomainRepository",
  "inputType": "IncreaseStockInput",
  "output": "Result<IncreaseStockOutput>",
  "domainModelNotes": [
    "InventoryItem is looked up by ProductId in the application layer.",
    "If no inventory item exists, the current implementation returns InventoryItemNotFound."
  ],
  "domainEvents": [
    "InventoryControl.Domains.DomainEvents.StockIncreased",
    "Lab.BoundedContextContracts.Inventory.IntegrationEvents.ProductStockIncreasedIntegrationEvent"
  ],
  "constructorPostconditions": [
    "Current stock is increased by the requested quantity.",
    "A ProductStockIncreasedIntegrationEvent is published after persistence."
  ],
  "requirements": ["INV-003", "INV-004"],
  "qualityUplift": "Current aggregate permits non-positive increase values; reconstruction must reject them without side effects."
}
