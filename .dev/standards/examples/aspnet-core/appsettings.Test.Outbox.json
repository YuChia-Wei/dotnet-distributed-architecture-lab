{
  "ConnectionStrings": {
    "PlanDb": "{{project-config:database.environments.test.url}}",
    "MessageStore": "{{project-config:database.environments.test.url}}"
  },
  "Database": {
    "Username": "{{project-config:database.environments.test.username}}",
    "Password": "{{project-config:database.environments.test.password}}",
    "Driver": "{{project-config:database.driver}}",
    "Dialect": "{{project-config:database.dialect}}",
    "Migrations": "{{TODO:project-config:database.environments.test.efCore.migrations}}",
    "EnableSensitiveDataLogging": "{{TODO:project-config:database.environments.test.efCore.enableSensitiveDataLogging}}"
  },
  "Repository": {
    "Mode": "outbox"
  },
  "Outbox": {
    "Enabled": true
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    }
  }
}
