# Backlog Items

Store one target-repository durable backlog item per `<item-id>.yaml` file in this directory, following `.dev/backlog/README.MD`.

Do not copy backlog items from the reusable AI context source repository. Every item retained here must cite evidence or a workflow that exists in `dotnet-mq-arch-lab`.
