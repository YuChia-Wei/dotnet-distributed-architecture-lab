# v0.16.0 Verification And Upgrade-Cost Report

## Metadata

- Workflow: `2026-09-06-ai-context-v0-16-0-upgrade`
- Issue: `YuChia-Wei/dotnet-distributed-architecture-lab#9`
- Edge: `v0.15.1 -> v0.16.0`
- Public Release: `v0.16.0` at `f1ead6d676193ba24d8517aed08f05fcfa23cbd3`
- Admitted package commit: `4d1a5c7d039618f007784679d9968c357347272b`
- Audit: `ASM-20260906-001`
- Applied checkpoint: `c0abe9c2ce288abf2b83edce1806fefb63b5886e`
- State: `finalized-terminally-validated-local`

## Final Result

- Public ZIP SHA-256 `d136b69e4153e7c85f892871fb0d3e6c5d8f88c7fd89d43fdb1b03ca88c5c85d`
  matched the hosted sidecar and package admission record.
- Incoming package validation passed for 650 manifest-covered files and 18
  portable entrypoints.
- The published direct route from v0.15.1 resolved without diagnostics.
- Final transaction `48d34abf...55f22` completed 70/70 conflict-free operations.
- Target-validation receipt bound a 34/34 passed run with output SHA-256
  `6209e9cdb803e0b1cd0ba35523346783e080562e88dcfe0d400f33a17b96b264`.
- Finalization published v0.16.0 provenance, four customizations, 13 effective
  rules, 20 route packets, and terminal receipt `5793b97d...d8bdc`.
- Final audit `ASM-20260906-001` found no blocking issue.

## Receipt Cleanup Boundary

The schema-2 pending receipt was 419,182 bytes and 8,812 lines. Its SHA-256
`fcec54829bcdbaee786ba1cf86330685733b483c7fa6383f873c51e9758509ad`
matched both the journal and terminal receipt before deletion. It was excluded
from checkpoint `c0abe9c2...5886e`, retained only for the first finalized gate,
then cleared after that gate correctly reported the sealed starting-HEAD
boundary. The journal-only rerun passed and proves that Git-admin terminal
evidence is now the durable transaction authority.

The removed transient file is recoverable from the sealed plan, journal, final
receipt identity, and package transaction evidence. It is not part of the
durable target context or final Git diff.

## Final Validation

| Check | Result |
| --- | --- |
| Provenance and terminal journal | passed; v0.16.0, effective readiness `ready` |
| Workflow artifacts | passed for 9 post-adoption workflows, 11 indexed workflow directories, and 1 backlog item before closeout; rerun is required over this completed locator |
| Dependency projection | passed; exact v0.16.0 target pins |
| Assessment artifacts | passed for 16 assessments |
| Shell assets | passed for 16 tracked assets |
| Target-owned tests | passed, 34/34 |
| Target Git commit policy | passed for the complete `main..HEAD` range |
| Product path scope | passed; no `src/**` or `tests/**` change |
| Formatting | `git diff --check` passed |

The report is terminal only after the same target command passes again over the
commit that contains this completed workflow:

```text
python -B .dev/ai-context/tooling/validate-target-ai-context.py --require-effective-rules --commit-range main..HEAD --workflow-id 2026-09-06-ai-context-v0-16-0-upgrade
```

Product build and tests were not rerun because neither product source nor its
test-result surfaces changed. This is impact-based validation, not a skipped
AI-context gate.

## Cost Comparison

| Edge | Operations | Observed package work | Conclusion |
| --- | ---: | --- | --- |
| v0.13.0 -> v0.14.0 | 188 | apply about 87 minutes | unacceptable historical baseline |
| v0.14.0 -> v0.15.0 | 50 | apply about 42-45 seconds | decisive engine improvement |
| v0.15.0 -> v0.15.1 | 6 | planning about 18 seconds | small patch; fixed scan dominates |
| v0.15.1 -> v0.16.0 | 70 | plan about 22.2 seconds; apply observations about 32.3-34.2 seconds | file update remains fast; supervised lifecycle still costly |

v0.16.0 therefore does not reintroduce the v0.14.0 file-write performance
problem. It does retain enough lifecycle friction that future upgrades should
continue recording plan/apply/finalization time and should not assume a
conflict-free plan guarantees a low-touch closeout.

## Remaining Follow-Ups

1. Make the package-native downstream validation profile closed over its
   published files instead of requiring 24 source-only omissions.
2. Emit target-owned projection work before sealing the transaction.
3. Provide one official schema-valid candidate serializer and canonical digest
   helper.
4. Reduce Windows executable-path rollback resume noise.
5. Add a first-class final-checkpoint/receipt-cleanup command and reduce the
   transient receipt footprint.

These follow-ups are documented in `ASM-20260906-001`; none blocks the current
local v0.16.0 installation. No push, pull request, merge, Issue closure, tag,
Release mutation, or publication was performed.
