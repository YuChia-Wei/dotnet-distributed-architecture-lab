# v0.12.0 Progressive Upgrade Reconciliation

## Candidate Status

Finalized v0.11 lifecycle checkpoint `00f4da7` and verified preflight
`ca17969` establish the exact predecessor. Official dry/apply plans are
byte-identical at SHA-256 `623d7904…`. The stock applier completed 73
replacements, five removals, and two additions with zero reconciliation or
ignored paths. Pending receipt `bd4d6dea…` binds 640 selected
framework-managed paths and records 80 applied operations, zero skipped IDs,
and no provenance update.

## Commit Grammar Transition

v0.12 policy schema 1.2 replaces the historical pipe meta-notation with exactly
one title discriminator: issue references or a scope. Its package-global
cutover predates valid v0.11 target history, so applying it literally would
retroactively invalidate 32 workflow commits. History rewrite is unauthorized.

The target overlay therefore records `ca17969` as the last legacy commit and
uses `2026-08-13T11:05:12+08:00` as the prospective subject boundary. From that
instant, `type(#1)` and `type(ai-context)` pass while `type(#1|ai-context)`
fails. Both root AGENTS translations now state these alternatives explicitly.
The existing AI-signature boundary and exact `ad194beb…` assessment waiver are
unchanged. Target GWTs pass 16/16; canonical policy tests pass 19/19; all 39
existing workflow commits pass the composed target policy.

`AICU-V012-COMMIT-CUTOVER-001` records the automatic-upgrade history break and
`AICU-V012-COMMIT-DOC-001` records the omitted release/migration guidance and
missing Markdown boundary explanation.

## Downstream Validation Reconciliation

`AICU-V010-PROJECTION-001`, `AICU-V090-DOC-001`, and
`AICU-V011-SELECTION-001` remain active. v0.12 expands source-only registry
declarations from fourteen to eighteen and adds
`AICU-V012-PROFILE-REGISTRY-PROJECTION-001`: the canonical profile-registry
test now unconditionally reads an absent source-only distribution profile.
That exact test is pinned as defect evidence and excluded from the downstream
gate; no package bytes or omitted source assets are fabricated.

The target gate adds the applicable lessons and canonical commit-policy tests.
The lessons suite first failed 6/6 in the sandbox solely because Windows denied
Temp fixture creation, then passed 6/6 outside the sandbox. Changed-path
profiles, evidence reuse, CI selection, and provider activation remain inactive.

## Effective Rule Transition

The thirteen rule statements and twenty route decisions are semantically
unchanged, but v0.12 updates selected-catalog source identity. Until audited
authority finalization regenerates every packet and publishes state last, the
existing v0.11 state must be treated as stale and routine action readiness must
remain fail-closed. The candidate gate accepts only the exact transitional
diagnostic `effective state catalogs[0] is stale`; any additional receipt,
authority, or state error still fails the gate. Three target tests prove exact
acceptance, additional-error rejection, and stderr rejection.

## Validation And Remaining Gate

Structural candidate `8fb457d6b8bb8086baadc1662bf138714550e102` uses the
new issue-only grammar. Its complete target-applicable gate passed outside the
sandbox in 33.9 seconds: receipt validation, applicable package checks, 31
target-owned tests, and all 40 workflow commits. `git diff --check` passes.

The first independent audit of clean metadata subject
`41a3a1dc984d84f6ebd8e0cae7f53b5a820fd984` independently passed all technical
gates in 33.8 seconds over 41 commits, but blocked finalization because the
durable evidence named a nonexistent structural SHA and still instructed the
next agent to commit synchronization that the subject already contained. This
metadata-only correction records the actual structural identity and preserves
that failed verdict. The next step is an independent `gpt-5.6-sol` / `max`
audit of the resulting clean metadata-correction HEAD. No CUST, provenance,
packet, or effective-state finalization occurs before a safe verdict. Merge,
push, Issue closure, Project mutation, profile/provider activation, and
upstream submission remain unauthorized.

The second independent audit accepted exact clean subject
`8437e178db96237edd7457bb48c531afb3bdae79` as `SAFE-TO-FINALIZE` after its
independent 34.1-second gate covered all 42 workflow commits. Assessment
`ASM-20260813-005` preserves that verdict and the earlier failed audit. The
sole next action is atomic v0.12 authority and effective-state finalization;
the working receipt remains until the finalized target gate succeeds.

Finalization completed at `2026-08-13T11:36:39+08:00`. Checkpoint
`e561b5c5f68629523fab616c39f226971399d429` advances provenance and all four
CUST records to v0.12, regenerates 20 packets and effective state with
action readiness `ready`, and removes only the working receipt after the
36.5-second receipt-present gate passed. The post-removal gate passed in 15.6
seconds, and the checkpoint commit passed policy validation across 44 commits.
The retained workflow receipt remains immutable evidence. The next operation is
exact-predecessor v0.13 preflight from the clean lifecycle-synchronization HEAD.
