# v0.11.0 Progressive Upgrade Reconciliation

## Candidate Status

The exact v0.10 predecessor authority at
`8284e97c6ae43af117f3c54687da9840b4e85e50` produced byte-identical dry/apply
plans (`9b0ac63a...`). The stock applier completed one addition and three
replacements with zero reconciliation or ignored paths. Pending receipt
`cfabfcfb...` binds 643 framework-managed paths.

## Framework Changes

v0.11 adds `PRODUCT-SOURCE-PROJECTION-CONTRACT.md`, updates the shared index,
and extends validation evidence with changed-path selection (`--base`/`--head`),
schema 2.0 selection/availability metadata, and workflow summaries. The new
contract establishes one canonical source tree while treating packages, staging,
and wrappers as projections and preserving target truth outside framework authority.

For this repository the stock profiles, changed-path runner, evidence reuse,
CI selection, and bundled provider remain inactive. Local routine validation is
still manual and CI remains unconfigured.

## Defect Reconciliation

- `AICU-V010-PROJECTION-001` is not resolved: source-only distribution,
  evaluation, registry, package-smoke, and closeout dependencies remain absent.
- `AICU-V090-DOC-001` is not resolved: the exact persistence guide still names
  the owner-retired analyzer test project.
- `AICU-V011-SELECTION-001` is new: direct changed-path matches are inserted into
  the selected set before dependency expansion; the recursive helper immediately
  returns for an already-selected ID, so declared dependencies may be omitted.
  The three projected tests most relevant to selector/evidence behavior are
  byte-identical to v0.10 and do not cover the new behavior.
- The older unchanged-required-path planner design is not repaired, but it does
  not trigger because finalized v0.10 proved 642/642 exact paths and v0.11 adds
  exactly one required path.

The version-pinned target gate records all three active defects without editing
framework bytes or claiming a stock profile pass.

## Validation And Finalization

The complete target-applicable gate passed outside the sandbox in 35.2 seconds:
643-path receipt validation, all target validators, prerequisite/evidence/profile
tests, effective rules/action skills, 21 target-overlay tests, and all 33 existing
commits passed. Existing effective-rule decisions and routes remain valid.

`git diff --check` reports only the package-native extra EOF blank line in the
new product-source projection contract. The receipt-bound byte is preserved and
the non-semantic diagnostic is recorded rather than silently normalized.

The structural candidate is committed as
`1bba9759eb51c19071d30aafc21825cbde439b25`. Request an independent read-only
`gpt-5.6-sol` / `max` audit of the resulting clean metadata-synchronization HEAD.
`ASM-20260813-004` records the resulting SAFE-TO-FINALIZE verdict. The governed
API then advanced provenance and all four CUST entries to v0.11, regenerated
twenty packets before publishing ready state last, and retained the exact
working receipt through the first complete gate. After that gate passed, only
the working receipt was removed and the complete gate passed again over all 36
commits. The durable receipt remains under this checkpoint's evidence folder.
Finalization checkpoint `197857e07c24e8d4fb8673ed959804e02b87d83a`
then passed the target commit policy over all 37 commits.

Merge, push, Issue
closure, Project mutation, activation, and upstream submission remain unauthorized.
