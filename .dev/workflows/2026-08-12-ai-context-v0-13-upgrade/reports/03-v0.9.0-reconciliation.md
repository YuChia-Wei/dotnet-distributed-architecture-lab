# v0.9.0 Progressive Upgrade Reconciliation

## Candidate Status

Structural candidate `4bf79fa6458b9f0c8eff05d08763432c1c270f91` is installed,
committed, and receipt-valid, but it is not yet finalized. A first independent
audit accepted metadata commit `1636f0a`; a stricter second audit then blocked
closeout because target-owned documentation still described the retired
analyzer/tooling projects, ignored build artifacts remained under `tools/`, a
premature assessment draft made the tree dirty, and an exact package guide
contained a dead tool-test command. The stricter verdict controls. Remediation
was committed as `5aeb00e85b9947b2b6b4305c707a37a8f2d4ed40`, and its
post-commit complete host target gate passed over all 18 workflow commits. The
next independent audit found that the remediation text itself named a
nonexistent `providers/` provider root. Commit `5a07a42` corrected all three
target-owned references to the installed `tooling/bundled-mechanical-validation/`
root and passed the complete gate over all 20 workflow commits. After the
durable checkpoint corrections, the independent audit accepted exact clean
commit `3d261f3b351bc6b656528d722de37779f1829016`; the SAFE-TO-FINALIZE verdict is
persisted as `ASM-20260813-002`. The customization ledger, provenance, packets,
and effective state advanced to `REL-v0.9.0` at `2026-08-13T09:50:24+08:00`
through the staged finalization API while the pending receipt remained active.
The finalized gate passed both before and after removal of only the working
receipt; the byte-identical workflow evidence copy remains retained. Commit
`8b02af138efe4fa3b02dbebabf9d1b67f7c1b68e` persists the complete checkpoint,
and its post-commit gate passed across all 25 workflow commits.

## Identity And Package Integrity

- From: `REL-v0.8.0`, tag `v0.8.0`, commit `97ccc9e9f218ec681bb726d2e1b4edbb3e14fb25`.
- To: `REL-v0.9.0`, tag `v0.9.0`, commit `c14a3260cba7d0a9e2b67b73df9e221280d2d2ef`.
- Published ZIP SHA-256: `2c98ac02eabd24ca881798caf83657adc2062ababe42fdb09fe26ce499cc98f2`; it matched the sidecar.
- Package payload: `652` files; declared payload digest `9ee8bf2175f9f58a7dd9a3a05c9987b599e1b0a8ffca617ad1e8ad8e5bc2de12`.
- Package metadata SHA-256: `7279faa5da0718b667103b46eb419054151dd67120ad41ff7d030fe4ffed75d2`.
- Migration metadata SHA-256: `ca0ec6f7d8694549a3cf6cfa6ef22bcfe7da88f7d7900b2c24e9f58c8f00d27a`.
- Final dry-run and apply plan SHA-256: `7acd05bd36a547e6f7cc3f93d9ddac3f7fa043c282de4df2df7711274bf0d715`.
- Pending/apply receipt SHA-256: `b5a87952fc3e6f59714b20f0df74cbf283072ba83652a799c672b8902d2039df`.
- The release declares breaking changes and supports automatic upgrade only from `v0.8.0`.

## Plan, Apply, And Required-Path Defect

The final plan from clean commit
`82f318f1ef8cc9ccc0500d7b52e6423ae1f131c0` contained `361` operations:
`218` safe operations and `143` explicit reconciliations. The stock applier
applied all safe operations, skipped all acknowledged reconciliations, and
created the schema-1.1 receipt with `637` required framework-managed paths.
All `143` reconciliations were then resolved: `142` framework paths received
exact incoming content and the sole target template, pull-request template
operation `migration-0345`, retained the target Issue/authorization contract.

Receipt validation then exposed a distinct v0.9 planner/receipt defect. Another
`159` required paths were unchanged between the v0.8 and v0.9 packages, so the
planner emitted no operation for them, while the receipt still correctly
required exact v0.9 bytes. The live split was:

- `127` CRLF/LF-only worktree differences;
- `2` whitespace-only differences; and
- `30` meaningful target semantic deltas.

After explicit owner authorization, a receipt-allowlisted reconciliation copied
only trusted v0.9 payload bytes, verified every expected hash before writing,
and recorded target-starting-commit blob, raw preimage, incoming, and result
hashes for every path. The pending receipt remained byte-for-byte unchanged and
now validates `637/637`. The repository adopted the v0.9 downstream seed
`* text=auto eol=lf` with `.bat`/`.cmd` CRLF exceptions so Windows checkout does
not recreate the same false conflicts.

## Semantic No-Operation Dispositions

Seven active behaviors were preserved outside receipt-bound paths before exact
replacement:

- target execution-provenance cutoff and the exact
  `ad194beb... / ASM-20260812-002 / missing-matching-trailer` attestation moved
  from the canonical commit policy/validator/test bundle into
  `.dev/ai-context/tooling/`;
- downstream applicability assertions replaced the three canonical source-only
  tests without modifying their exact published bytes.

The incoming workflow policy, workflow validator, and lifecycle tests were
adopted exactly. Their earlier cutoff causes no current loss because all active
tasks already carry model and reasoning provenance; the old target-only cutoff
is therefore superseded rather than falsely retained in the ledger.

The remaining meaningful documentation/governance deltas were explicitly
adopted or projected into target-owned truth:

- audit/governance boundary details remain in `AGENTS.md`, the workflow, and
  customization evidence while canonical skill text adopts v0.9;
- Issue-first and authorization settings remain in `.dev/project-config.yaml`,
  the pull-request template, and root guides;
- `requirement-author`, `spec-author`, and compatibility-alias routing remains
  target-owned in `AGENTS.md` while canonical guides adopt current names;
- repository identity, operational truth, technology selection, and historical
  source/package notes remain in target-owned indexes, architecture/config,
  workflow evidence, and reports rather than modified framework paths.

The exact per-path record is
`../evidence/v0.9.0/post-apply-required-path-reconciliation.yaml`; the three
owner decisions enumerate adoption, analyzer retirement, rehome, and package
defect boundaries.

## Analyzer And Provider Decision

Live evidence showed that the historical analyzer was active through
`src/Directory.Build.props`, solution membership, and DBA severity settings.
The owner instructed that an active analyzer be removed completely. Commit
`82f318f` therefore removed analyzer/validation projects and source, build
wiring, solution entries, severity settings, and project-config inventory.
The follow-up audit found ignored historical `bin` and `obj` output beneath the
retired root; those verified generated artifacts and their resulting empty
parents were then removed. Target-owned root guides, architecture truth, and
project configuration now state the same retirement boundary. The solution
still builds with `0` errors and six pre-existing nullable warnings. v0.9's
bundled mechanical-validation implementation is installed only as an inactive
`source-available` provider; no provider cutover or activation was authorized.

## Target Validation Overlay And Package Projection Defect

The v0.9 payload is internally inconsistent as a downstream projection:

- `python-entrypoints.json` declares `13` portable and `13` source-only CLIs,
  while twelve source-only implementations are intentionally not packaged;
- canonical tests read the absent
  `.ai/distribution/profiles/dotnet-backend.yaml`;
- `validate-ai-context.py` and `check-all.sh` unconditionally reference absent
  source release, evaluation, distribution, repository, and retired tool-test
  surfaces.
- the exact persistence configuration guide still tells downstream users to
  run `tools/DotnetBackendValidation.Tests`, which the same owner-approved v0.9
  migration retired.

Canonical files remain exact. The dead documentation command is recorded as
`AICU-V090-DOC-001` and covered by a target-owned negative assertion; the
receipt-bound guide remains unchanged. The target gate is version-pinned by SHA-256,
fails if a dependency drifts, verifies the declared downstream projection, and
runs only applicable target/provenance, workflow, dependency, assessment,
shell, Python prerequisite, effective-rule, action-skill, and target overlay
checks. Stock `validate-ai-context.py` and `check-all.sh` are recorded as
`blocked-by-package-projection`; they are not claimed as passed.

## Effective Rules And Initial Settings

The target explicitly accepts all `13` catalog rules as
`baseline-effective`. It defines `20` exact, wildcard-free action routes and
loads the complete ordered rule set for each because v0.9 catalogs declare the
source selectors unconstrained and prohibit invented narrower predicates.

Two potentially ambiguous choices were resolved from repository evidence:

- `TEST-BDDFY-001`: select plain xUnit with mandatory recognizable GWT
  semantics, the baseline-permitted repository-wide BDDfy opt-out;
- `DELETE-PURGE-001`: retain baseline applicability because Inventory contains
  executable physical SQL deletion; record missing retention/authorization/
  audit policy as debt rather than hide it as not applicable.

The full candidate, evidence, route IDs, and known compliance debt are in
`../evidence/v0.9.0/effective-rule-decisions.yaml`. Staged finalization wrote the
audited ledger/provenance bytes, derived their authority digests, published all
twenty packets first, and published the live state last. Readiness is `ready`.

## Candidate Validation

- Receipt validation: passed for all `637` required paths.
- Target provenance validation with `--allow-unfinalized`: passed; readiness is
  correctly unresolved only because effective state is not finalized.
- Workflow, dependency, assessment, and shell validators: passed.
- Python prerequisites: `14/14` passed outside the sandbox.
- Effective-rule resolver: `18` passed; two Windows symlink-privilege cases
  explicitly skipped.
- Action-skill effective-rule contract: `3/3` passed.
- Target downstream/exception/effective-decision overlay: `19/19` passed. The
  first route-ID oracle omitted the canonical terminal LF and failed all
  twenty correct IDs; correcting only that target-owned test restored parity
  with the v0.9 framework resolver (`20/20`).
- Target commit validator: all `15` workflow commits passed.
- Aggregate target gate: passed outside the sandbox in `29.4` seconds.
- Post-candidate aggregate target gate: passed outside the sandbox in `28.1`
  seconds, including the complete `16`-commit workflow range.
- Solution build after analyzer removal: passed with `0` errors and six
  pre-existing nullable warnings.
- Independent assessment `ASM-20260813-002`: SAFE-TO-FINALIZE for exact clean
  commit `3d261f3b351bc6b656528d722de37779f1829016`.
- Finalized target gate: passed with the pending receipt and passed again after
  removing only the working receipt; 13 dispositions, 20 routes, 20 packets,
  and the complete 24-commit range were verified.

`git diff --check` reports the two exact package whitespace-at-EOF changes. They
are retained because changing them would invalidate receipt identity and are
recorded as upstream package feedback, not relabelled as a clean check.

## Remaining Gate

The v0.9 checkpoint is finalized and its post-removal read-back passed. The next
allowed operation is exact-predecessor v0.10 planning from finalized v0.9
provenance. Merge, push, Issue closure, Project mutation, provider activation,
and upstream submission remain unauthorized.
