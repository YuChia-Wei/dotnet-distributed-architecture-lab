# v0.10.0 Progressive Upgrade Reconciliation

## Finalized Status

The exact-predecessor v0.10 package was planned from finalized v0.9 commit
`c9cc01790b9692d22c6f7dc8238a13828cbd703d`. Dry-run and apply plans are
byte-identical (`e567b74e...`): 16 replacements and five additions, with zero
reconciliations and zero ignored framework paths. The stock apply created a
pending receipt covering 642 required framework paths (`8056ef55...`).

The candidate was receipt-valid and passed the target-applicable gate.
`ASM-20260813-003` accepted clean subject `3388f25...`; the governed finalization
then advanced provenance and all four CUST entries to v0.10 and regenerated all
twenty packets before publishing ready effective-rule state.

## New Framework Behavior

v0.10 introduces named validation profiles (`fast`, `pr`, `release`,
`closeout`, `nightly-full`), retained validation logs, privacy-minimized JSONL
evidence, reuse-by-input metadata, per-check timeout/resource/environment
records, a validation evidence helper, and a profile registry. Legacy profile
aliases now map `quick` to `pr`, `critical` to `release`, and `full` to
`nightly-full`.

For this target these features are installed but not activated as authoritative
automation. The current settings remain:

- local routine validation: `manual`;
- CI routine validation: `unconfigured`;
- stock profile runner: inactive;
- validation evidence reuse: inactive;
- bundled mechanical-validation provider: source-available and inactive;
- repo-backlog provider: enabled from target provenance.

## Projection Defect Status

The v0.9 package-projection defect is not resolved and expands in v0.10:

- the new package smoke test imports absent source-only
  `test_ai_context_packaging.py`;
- stock validation still expects omitted distribution/evaluation/source assets;
- the registry now declares 14 source-only entrypoints, of which 13 are absent
  from the target package projection;
- the closeout profile and guide index reference absent release-closeout test,
  CLI, and guide assets;
- the carried exact persistence guide still names the owner-retired tool test.

The target overlay is repinned to v0.10, adds the applicable validation-evidence
and profile-registry tests, and records `AICU-V010-PROJECTION-001` plus carried
`AICU-V090-DOC-001`. It does not patch framework bytes or claim stock
`check-all.sh` success.

## Validation And Experience Notes

- The first stock apply was denied at `.agents/skills/README.md` by sandbox ACL;
  the applier rolled back fully. The identical operation passed outside sandbox.
- New validation evidence tests failed in sandbox Windows Temp ACL; profile
  tests failed on Git Bash signal-pipe creation. Outside sandbox they passed
  `5/5` and `4/4`.
- The first candidate gate correctly rejected the unstaged new shell helper
  because its required Git mode `100755` was not observable. After staging the
  package mode, the complete target gate passed in `34.7` seconds.
- Receipt-bound target validation reports effective-rule readiness `ready` and
  the complete target commit-policy range passes.

## Checkpoint Handoff

The structural candidate is committed as
`d5a80b12a3e877fc3fc27dd814f6a9f1204e2380`. Request an independent read-only
`gpt-5.6-sol` / `max` audit of the resulting clean metadata-synchronization
HEAD. `ASM-20260813-003` records the resulting SAFE-TO-FINALIZE verdict. The
governed API finalized v0.10 at `2026-08-13T10:32:23+08:00`; target gates passed
both before and after working-receipt removal. Finalized checkpoint
`d8cff8214121128f43d3daec75836c1147bb642a` passes the 32-commit target policy;
continue with v0.11 from the resulting clean lifecycle-synchronization HEAD.
Merge, push, Issue closure, Project mutation, profile/provider activation, and
upstream submission remain unauthorized.
