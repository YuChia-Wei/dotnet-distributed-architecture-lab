# v0.13.0 Reconciliation

## Status

`finalized-ready`

The published v0.13 package and its exact v0.12 predecessor contract are
verified and structural candidate `a8cb51d5e8ca856cbc22c87b4f4c18a26b778239`
is committed. The owner-authorized bounded `AGGREGATE-ES-001` product fix was
implemented and audited, after which v0.13 provenance, all four CUST records,
twenty route packets, and effective state were atomically finalized.

## Package And Migration

- ZIP/sidecar SHA-256: `092cd9e49de366458cafb304ca63b4c8f028b03e8cf7f718b06d8710a98efcfd`
- Source commit: `8584337b47295da1af914180baf2b3f815b9dcc7`
- Payload: 633 files; 638 checksum entries; zero mismatches
- Migration: 133 operations = 80 replace + 36 remove + 15 add + 2 reconcile
- Exact predecessor: finalized `REL-v0.12.0`
- Receipt: 619 required framework paths, with repo-backlog retained enabled

The first extracted-package validation encountered two generated Python bytecode
files outside `SHA256SUMS.txt`. Only those exact `__pycache__` files were
removed, bytecode writes were disabled, and package-root validation then passed.
This repeats the v0.12 extraction-hygiene friction and is retained for upstream
feedback rather than misclassified as package-content corruption.

The operation list and v0.12-to-v0.13 payload delta match exactly at digest
`fcf36e7f44cae91d85ecf99db7b56646d01a92c6a78d996f14f923415faf16`.
The official dry-run and apply plans are byte-identical at SHA-256
`96d5fec2430fae24fb278cb491ff9b6c938cf68eeaff6764990f78e9d8c1d308`.
From clean commit `2c94085038c678a368bc94b846667dfcec5e88cc`, the applier executed exactly
131 operations and skipped only acknowledged `migration-0132` and
`migration-0133`. Pending receipt SHA-256 is
`094840edc10744c12397a905e1783f8200a498081a492d5fae0f13f41b979ec0`.

## Authorized Reconciliations

`migration-0132` keeps the target PR template's Issue/authorization evidence
while merging v0.13 delivery/integration and repository-native validation
prompts. `migration-0133` retains target `global.json` SHA-256
`1c2fa3029018c600f2d1546d1a22bfb625467d88cf1a9c42e17649777526fa40`:
the framework is SDK-neutral, while this repository remains a .NET 10 target.

The 36 bundled mechanical-validation/provider files are removed and six
reference-only recipe assets are added. Presence does not activate them. The
recipes remain `not-selected`; no analyzer project, solution reference,
severity configuration, CI profile, or runtime provider is created.

## Later-Version Conflict Resolution

The retired provider conflict is automatically classified
`superseded/resolved-by-v0.13.0`, consistent with the owner's rule that a later
exact package resolution does not require a repeated decision. Likewise,
`AICU-V090-DOC-001` is resolved by the v0.13 persistence guide. Other defects
remain active unless exact package evidence proves resolution.

## Remaining Projection Defects

The stock target projection still assumes omitted source assets. The new
portable code-review routing test also imports absent source-only
`ai_context_package.py`, so it cannot be claimed as passing downstream. The
target-owned gate pins these failures, runs an applicable static routing
contract for downstream GWT1–7, and keeps GWT8 source-release-only. Its 41
tests pass. The exact stock validator still exits 1 with 36 downstream
projection errors (16 Python registry, 17 shell registry, and 3 catalog/index),
while the stock routing test exits before discovery with
`ModuleNotFoundError: ai_context_package`. Stock profiles, changed-path
execution, evidence reuse, and analyzer recipes remain inactive.

The composed candidate target gate passed outside the sandbox before
finalization. It verified the pending receipt, exact
framework pins, target projection, workflow/assessment/dependency/shell
contracts, and commit history while accepting only the one declared stale
catalog diagnostic.

## Product-Semantic Decision Gate

`AGGREGATE-ES-001` remains `baseline-effective`. The audit discovered that the
former target `EsAggregateRoot(IEnumerable<IDomainEvent>)` called virtual
`When` from its base constructor and `Order(events)` delegated replay through
`base(events)`. The rule requires a protected replay path invoked from the
derived constructor body. v0.13 changes validation ownership wording but did
not itself resolve this construction invariant.

The owner approved the recommended bounded product fix at
`2026-08-13T12:30:13+08:00`: add protected
`Replay(history)` to the aggregate base and call it from `Order`'s derived
constructor, with narrow replay regression coverage. The approval is recorded
in `../evidence/v0.13.0/owner-decision-product-fix.yaml`; it does not authorize
weakening the rule. The bounded fix passed its task packet, product validation,
and independent audit gates before v0.13 authority was finalized.

The live v0.13 production and test packet resolvers both fail closed with the exact
diagnostic `effective state catalogs[0] is stale`. This cannot be repaired
in place before the product fix without falsely finalizing v0.13 authority: candidate
output is explicitly non-activating, and an isolated target packet is not this
repository's task-scoped authority. The selected governed route temporarily
uses finalized v0.12 checkpoint `d3c6a032`, whose fresh production/test packets
already carry the same construction invariant, then cherry-picks only the
bounded product commit into v0.13. An initial uncommitted draft was fully
restored to the three HEAD blobs before this governed action; its sandbox test
never executed because `obj` was not writable. Evidence is retained in
`../evidence/v0.13.0/preaction-packet-analysis.yaml`. Both real v0.13 packets
must still resolve and be post-checked immediately after finalization.

The detour resolved both v0.12 packets before the actual mutation. Each loaded
the full thirteen-rule set from target-state digest `8c85750c...`; packet
digests are `2ce8bae3...` for C# production and `f01d1a67...` for C# tests.
Commit `886dcca` introduced only the aggregate base, its direct `Order` caller,
and the direct domain test; v0.13 commit `aff588e` is its single cherry-pick.
The domain suite passed 13/13 on both branches, the full solution built with
zero errors on the remediation branch, and the v0.13 target gate passed outside
the sandbox in 35.3 seconds over 50 first-parent commits.

Finding `AICU-V013-AGGREGATE-CONSTRUCTION-001` is therefore resolved pending an
independent fixed-HEAD audit. `ASM-20260813-006` independently accepted clean
subject `a3389994bf52f9265b7fe0a079ccd4041efc8997` as
`SAFE-TO-FINALIZE`. `AGGREGATE-ES-001` remains `baseline-effective`:
the product now conforms to the exact baseline, so no semantic-delta CUST is
created.

## Authority Finalization And Packet Post-Check

At `2026-08-13T12:59:17+08:00`, the governed finalizer validated the unchanged
working receipt, advanced provenance to `REL-v0.13.0`, reconciled all four
existing CUST records, derived the thirteen explicit rule dispositions, and
published twenty route packets before publishing ready effective state. The
first finalized target gate passed outside the sandbox with the working
receipt still present.

Both exact v0.13 `local-change/direct/dotnet-backend` packets then resolved with
freshness `verified`, target-state digest `ec489634...`, the complete
thirteen-rule set, and `AGGREGATE-ES-001: baseline-effective`. Production used
packet `PACKET-50E12D...` with digest `3284502d...`; test used packet
`PACKET-8AEC71B...` with digest `071b26bc...`. Only after these checks passed was
the working `.dev/AI-CONTEXT-APPLY-PENDING.yaml` removed. The durable receipt
under `evidence/v0.13.0/` remains byte-identical at SHA-256 `094840ed...`, and
the complete target gate passed again outside the sandbox in 16.8 seconds over
52 first-parent commits.

## Post-Finalization Target-Truth Correction

The independent finalization audit accepted clean checkpoint `a003618`, but the
subsequent developer-impact and discussion-branch analyses found a target-owned
documentation contradiction outside the receipt-bound framework payload.
`README.md`, `README.en.md`, `.dev/ARCHITECTURE.md`, and
`.dev/project-config.yaml` still described the removed bundled provider as
source-available. Exact v0.13 package evidence instead proves that only
reference-only, `not-selected` assets remain under
`.ai/assets/tech-stacks/dotnet-backend/tooling/on-demand-mechanical-validation/`.

The four target-truth files now describe the installed recipe-only state, and
the downstream projection suite rejects the obsolete provider path across all
four surfaces. The direct suite passed 12/12 and the complete target gate passed
outside the sandbox in 17.0 seconds over 54 commits. This is classified as
`AICU-V013-TARGET-PROVIDER-TRUTH-001`, a target customization/evidence-sync
defect rather than an upstream package defect. It changes no framework-managed
v0.13 bytes and does not select, activate, or wire any provider. Independent
audit of the resulting clean fixed HEAD remains required before closeout.

The first remediation audit bound clean commit `d985e0ef24d8716481ca7dc323472cf19545103f`.
All technical and provider-state checks passed, including the focused 12/12
suite and complete target gate over 55 commits, but the audit correctly returned
`BLOCKED`: active resume text still instructed the already-completed `d985e0e`
commit and the branch lifecycle omitted that checkpoint. This failed audit is
retained. The following metadata synchronization changes no provider or
framework content; a fresh clean fixed-HEAD audit is the only next action.

The fresh audit accepted clean metadata-synchronization checkpoint
`46928f4379f57a7bec155056aacdf6dbcf070f81` as `SAFE-TO-PROCEED`. It verified
the four target-truth surfaces, old-provider absence, six reference-only recipe
files, 12/12 focused tests, 41/41 target tests, all 56 commits, unchanged
REL-v0.13 authority, and no audit residue. The remediation is therefore closed
and becomes the fixed subject for post-upgrade analysis.

## Authorization Boundary

No merge, push, Issue closure, Project mutation, provider/profile activation,
history rewrite, or upstream feedback submission is authorized.
