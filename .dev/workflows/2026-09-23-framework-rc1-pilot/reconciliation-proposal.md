# Target reconciliation proposal

Status: direct owner decision recorded below; local integration prepared for independent review. Installation and current routes exist; activation remains pending.
Owner: ai-context-governance / coordinator task 01a0ce78-db26-74e1-a615-2bd0599f7d0c.
Input baseline: cb122285af09e46b24102f22a0ad9d713219c1f2; Issue #15.

## Preserve semantic authority

Retain the exact 194 .NET assets, both engineering-rule catalogs, all 14 effective
rule dispositions and their existing packets. Retain current provenance and
customizations bytes as the explicitly named v0.18 inherited rule/legacy-package
baseline. They cannot truthfully identify an unpublished versioned candidate:
the legacy parser requires REL-vX.Y.Z, a matching tag, two old mandatory cores
and completed release-migration evidence. Do not manufacture any of those facts.
The new .ai/framework.lock is the managed candidate identity. A separate target
adoption/binding record will bind it to retained target rule/customization
identities, exact root/entry changes, check results and explicit activation.
Existing 20 rule selectors remain target-owned; new instruction routes explicitly
consume the relevant target rules instead of claiming a built-in .NET extension.

| Customization | Retained meaning | Necessary integration |
| --- | --- | --- |
| CUST-DOTNET-MQ-GOVERNANCE | Issue-first execution, repo facts, LF and scoped runtime tracking | Root links and current Codex registry point to new entries; preserved legacy duties named |
| CUST-DOTNET-MQ-VALIDATION | Exact target applicability, pinned dependencies and truthful outcomes | Separate current candidate/layout checks from historical v0.18 package projections |
| CUST-DOTNET-MQ-REPO-TRUTH | Product architecture, bounded-context choices, inactive retired tools | Preserve product/config truth; no product code or tooling resurrection |
| CUST-DOTNET-MQ-EXECUTION-PROVENANCE-ADOPTION | Actual execution attribution, prospective subject grammar, exact one historical waiver | Keep target timestamps, SHA/diagnostic waiver and Git overlay semantics |

No new framework instruction supersedes target semantic authority by itself.
Preserving old bytes does not automatically prove applicability or successful
new consumption; the proposal needs actual new route/rule checks and post-change
independent context verification before activation is recorded.

## Runtime entries

Archive the 13 superseded Codex and 13 superseded Claude entry directories into
version-scoped target history outside active skill discovery, preserving exact
bytes and recorded Git blob identities. Activate only the new 18 generated Codex
entries after managed/read/config checks. Keep ai-context-init/upgrader entry
responsibilities explicitly restricted to legacy published-package and original
recovery formats, not ordinary new-framework initialization/update.
Claude's root notice states that the new framework is unsupported; no generated
Claude adapter exists. Old .NET source/reference assets remain available as
explicit target rules, not as alternative ordinary capability entry points.

## Validation boundaries requiring a target decision

The existing target gate hard-codes v0.18.0, six old validator paths/hashes and
all target test discovery. Its projection checks bind registry counts, old
canonical skills and both runtime wrappers. Renaming the manifest version or
just dropping --require-effective-rules is insufficient: existing effective
state is always validated and depends on exact historical provenance/ledger.

Proposed current pilot gate keeps exact candidate/core/runtime/lock byte checks,
selected Codex and withdrawn Claude route checks, all 14 rule/disposition and
packet freshness checks, unchanged .NET asset bindings, all four customization
subjects, target Git grammar/attribution, relevant workflow/assessment/dependency/
shell checks, and independent target-context verification. It must fail on any
missing required current check or unresolved semantic preservation.

Keep the existing v0.18 gate/tests as version-scoped historical/package support;
report their original coverage rather than calling them new-framework passes.
The historical v0.9 effective-rule test covers 13 rules and must not stand in for
the live 14-rule preservation check. Source U001/CI suspension is never exported.
Changing which gate is the target's current required check is a target policy
adoption decision. This proposal does not silently retire a required check.

The alternative is to keep the current v0.18 gate as the required current gate
throughout the pilot. That permits staging new managed resources but leaves full
route activation pending wherever obsolete wrapper/package assertions conflict.
No complete-adoption claim is allowed in that staged state.

## Evidence used and limits

The bounded read-only target_gate_inventory worker inspected named tracked paths
and returned exact function/line references. Its graph was unavailable, so it
used tracked-file fallback. No tests/validators/provider actions ran and its
analysis is not an independent adoption audit. Parent checked the baseline
identity and preserved tooling/authority bytes while creating only own records.
Key evidence: load_manifest/build_commands in validate-target-ai-context.py;
validate_target in ai_context_target_provenance.py; _validated_target_authorities,
_load_state_inputs and _validate_packet in ai_context_effective_rules.py;
test_code_reviewer_routing_projection.py and test_effective_rule_decisions.py.

No root, authority, runtime, validator or managed file has been changed by this
proposal. Native installer readiness remains separately pending source #382.


## Direct target gate adoption decision

The owner directly answered call_9Yd5ZfJdp1eBXVZf4Vp09U1v:
"採用新佈局相符的目標 gate：保留 14 條規則、四項客製化、精確雜湊、Git 歸屬與獨立審查；舊 v0.18 gate 保留為歷史／舊版支援（建議）".
This adopts the proposed validation boundary, not a passing execution result.
Current candidate identity will come from .ai/framework.lock and the active
target binding; retained legacy provenance/effective state identify only the
v0.18-derived target rule/legacy support baseline. Root and active consumers
must consult that binding; a passive side record is insufficient. Existing
selector/packet equality and unresolved fail-closed behavior remain required.
No rc version is inserted into the old release-only provenance schema.
