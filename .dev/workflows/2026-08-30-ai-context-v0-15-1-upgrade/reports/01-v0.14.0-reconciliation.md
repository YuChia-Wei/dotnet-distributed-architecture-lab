# v0.14.0 Reconciliation Report

## Metadata

- Workflow: `2026-08-30-ai-context-v0-15-1-upgrade`
- Issue: `YuChia-Wei/dotnet-distributed-architecture-lab#3`
- Edge: `v0.13.0 -> v0.14.0`
- Package commit: `412bb14a16fe75ee65a020b16680def0acc0ff1b`
- Transaction start commit: `32140f52ffffd14dc235b310d386d6c1b3765e70`
- Recorded at: `2026-08-30T20:21:40+08:00`
- Current state: `awaiting-target-validation`

## Apply Result

- The digest-bound transaction applied all `188 / 188` automatic operations.
- The apply began at approximately `2026-08-30T18:46:34+08:00` and completed at approximately `2026-08-30T20:13:00+08:00`, taking about **87 minutes** on the Windows target.
- Operation distribution was `125` software-core, `51` lifecycle, and `12` dotnet-backend operations.
- Three owner-approved reconciliations were preserved instead of overwritten:
  `.editorconfig`, `.github/pull_request_template.md`, and `AGENTS.md`.
- The package receipt remains `pending-validation`; provenance and the customization ledger have not been finalized.

## Target Reconciliation

The v0.14 downstream package removes six tests that the v0.13 target gate had
selected, while its active Python and shell registries still reference removed
or source-only paths. Consequently, the stock `validate-ai-context.py` and
`validate-shell-assets.py` routes are not package-closed and are not claimed as
passing.

The repository-owned target projection was version-pinned to the exact v0.14
package identities and changed only target-owned surfaces:

- `.dev/ai-context/tooling/target-gate-manifest.yaml` records the package-native
  projection defect, registry counts, removed checks, and receipt-cycle
  disposition.
- `.dev/ai-context/tooling/target-validation-policy.yaml` binds the v0.14
  package policy and commit-validator bytes while retaining the target's
  prospective commit-policy boundary.
- `.dev/ai-context/tooling/validate-target-ai-context.py` omits only the target
  provenance sub-check during `--allow-unfinalized`, then restores it for the
  final gate. This breaks the package transaction's receipt/provenance cycle
  without weakening final validation.
- The downstream projection tests now assert the exact v0.14 applicability
  boundary and the absence of removed tests.
- `/.dev/ai-context/local/` is explicitly ignored as required by the package.

## Validation

The following target command passed outside the sandbox in approximately 6.4
seconds:

```text
python -B .dev/ai-context/tooling/validate-target-ai-context.py --allow-unfinalized --workflow-id 2026-08-30-ai-context-v0-15-1-upgrade
```

It passed workflow-artifact, dependency-version, assessment-artifact, and all
`33` target-owned tests. The generic framework validator remains non-passing
because of the package projection defect and is deliberately excluded from the
target pass claim.

## Performance Gate For Later Edges

The **87-minute / 188-operation** v0.14 apply is the comparison baseline for
v0.15.0 and v0.15.1. Before either later edge is applied, the workflow must
inspect its operation count and distribution, transaction durability strategy,
and measured planning/dry-run cost. If v0.15.x still performs materially similar
per-file full-state durable updates without a clear improvement, the workflow
must stop before apply and track that update-cost defect separately.

## Remaining Gates

1. `ASM-20260830-001` is published locally and its structured artifacts pass.
2. The exact target-validation execution passed and a canonical receipt was
   produced, but the recorder rejected the required post-apply target overlay
   as unrelated changes. Resolve the lifecycle-order blocker before binding it.
3. Finalize v0.14 provenance and the semantic customization ledger without
   changing the sealed candidate authority documents.
4. Regenerate and validate effective rules if final authority publication makes
   the existing state stale.
5. Run the final target gate before creating a v0.14 checkpoint commit.
