# Gemini Command Wrappers

本目錄放置 Gemini CLI 可讀取的薄 wrapper commands。
若未來不再使用 Gemini wrappers，本目錄可直接移除，不影響 canonical source。

## 原則

- 保持薄 wrapper
- 指向 `.ai/assets/` canonical source
- 不在這裡重複完整 prompt package
- 目前主要作為 legacy / compatibility wrapper layer，而不是 canonical command-spec 的主入口

